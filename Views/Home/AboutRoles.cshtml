@model List<String>
 

@{
    ViewBag.Title = "AboutRoles";
}

<h2>AboutRoles</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            
        </th>
        <th>
            
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item)
        </td>
        <td>
        </td>
        <td>
        </td>
    </tr>
}

</table>
