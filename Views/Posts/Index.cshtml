@model IEnumerable<DomainModel.Entity.Post>

@{
    ViewBag.Title = "Почтамты";
}

<h2>Почтамты</h2>

<p>
    @Html.ActionLink("Добавить новый", "Create")
</p>
<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.post)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.post)
                </td>
                <td>
                    @Html.ActionLink("Редактировать", "Edit", new { id = item.post }) |
                    @Html.ActionLink("Детально", "Details", new { id = item.post }) |
                    @Html.ActionLink("Удалить", "Delete", new { id = item.post })
                </td>
            </tr>
        }
    </tbody>

</table>
